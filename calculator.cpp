#include<iostream>
using namespace std;
int sub(float a,float b){
    return a-b;
}
int main(){
    float a, b, result;
    result=add(a,b);
    result=sub(a,b);
    result=multi(a,b);
    result=divide(a,b);
}